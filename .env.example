# Server-side secrets — never commit this file
ANTHROPIC_API_KEY=sk-ant-...

# Master admin key — used to create/revoke user API keys
# Change this to something long and random before deploying
GUARDRAIL_MASTER_KEY=gr_master_changeme

# Optional: override default port (default: 3001)
# PORT=3001
